export namespace MilliSec {
    export const ONE_SEC = 1000;
    export const ONE_MIN = ONE_SEC * 60;
    export const ONE_HOUR = ONE_MIN * 60;
    export const ONE_DAY = ONE_HOUR * 24;
    export const ONE_WEEK = ONE_DAY * 7;
}
